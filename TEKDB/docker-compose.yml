version: '3.8'

services:
  web:
    build: /usr/local/apps/TEKDB
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./app/:/usr/local/apps/
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev

